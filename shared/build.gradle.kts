// shared — 컨텍스트 간 주고받는 "통합 이벤트 계약"만 담는 얇은 공용 모듈. (architecture.md §4)
// 의존성을 의도적으로 0으로 둔다: 비즈니스 로직/프레임워크가 새어들면 "분산된 모놀리스"가 된다.
// (이벤트는 순수 record. 직렬화 설정은 각 컨텍스트의 infrastructure 책임 = C-4)

// 추가 의존성 없음 — java-library 기본만 사용.
