plugins {
    id "org.gradle.toolchains.foojay-resolver-convention" version "0.9.0"
}

rootProject.name = "order-platform"

include(
    ":shared",
    ":order",
    ":payment",
    ":inventory",
    ":bootstrap",
)
